#include <stdio.h>
//no se puede usar for,while,do,strlen, etc
//
void mostrar_invertido( char* ) {
  if(str[i] == 0)
    return;
  printf("%c", str)
}


int main() {
  mostrar_invertido(argc>1?argv[1]:"Nada",0);

  return 0;
}
